mode: DEV

classes:
      packages: ["ru.frostman.web.test", "ru.frostman.web.test2"]
      updateInterval: 500

templates:
      path: "/WEB-INF/view"

app:
      asyncQueueLength: 100

plugins: [
      "ru.frostman.web.aop.AopPlugin",
      "ru.frostman.web.MongoPlugin"
]

#res:
#      jquery: true
